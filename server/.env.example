# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional
PORT=8787
# Optional: override the model used for extraction (must support vision + tool use)
ANTHROPIC_MODEL=claude-sonnet-5
