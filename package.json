{
  "name": "prevent-risk-suite",
  "version": "1.0.0",
  "private": true,
  "description": "ASCVD / CVD / Heart Failure risk calculator (AHA PREVENT equations) with local, offline-friendly lab/data extraction and guideline-based recommendations.",
  "scripts": {
    "install:all": "npm install --prefix client",
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client"
  }
}
