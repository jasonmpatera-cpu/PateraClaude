{
  "name": "prevent-risk-suite",
  "version": "1.0.0",
  "private": true,
  "description": "ASCVD / CVD / Heart Failure risk calculator (AHA PREVENT equations) with AI-assisted lab/data extraction and guideline-based recommendations.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c red,white \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
